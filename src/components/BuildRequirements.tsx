export function BuildRequirements() {
  return (
    <details className="card">
      <summary>Build requirements (from the teaching team)</summary>
      <ul>
        <li>At least 3 houses.</li>
        <li>At least 3 apartment compounds (each roughly 3x the size of a house).</li>
      </ul>
      <p className="hint">
        These are minimums for the physical city, not story categories — keep writing stories as citizen
        needs (e.g. &quot;As a family, I want an apartment near transit...&quot;) and make sure the backlog
        covers enough housing to hit these counts.
      </p>
    </details>
  )
}
